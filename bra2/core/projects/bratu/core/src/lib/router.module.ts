import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';

const RouterModules = [
    RouterModule
];

@NgModule({
  imports: RouterModules,
  exports: RouterModules,
})
export class BratuRouterModule {}
